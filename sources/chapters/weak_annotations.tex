\chapter{Annotating syntetic data with \glspl{llm} for Multilingual Clinical Entity Extraction}
\label{chap:distillation}

In clinical and other specialized domains, data are scarce due to their confidential nature. This lack of data is a major problem when fine-tuning language models.
Nevertheless, very large language models (\glspl{llm}) are promising for the medical domain but cannot be used directly in healthcare facilities due to data confidentiality issues. We explore an approach of annotating training data with \glspl{llm} to train smaller models more adapted to our problem. 
We show that this method yields promising results for information extraction tasks.

\minitoc

\section{Introduction}
Clinical notes contain the interactions between the patient and healthcare staff. Professionals record their impressions, observations, and various medical procedures performed. Despite the computerization of clinical documents, notes should remain fairly expressive and in a free format to save time for healthcare personnel and allow for the description of unusual situations \citep{rosenbloomDataClinicalNotes2011}. Moreover, a large amount of crucial information is exclusively contained in clinical notes. According to a study by \citet{Escudie2017ADisease}, approximately 80\% of patient phenotypes (a set of observable biological and physical characteristics that can characterize a disease) are present only in free text. These documents are difficult to use without advanced methods such as deep learning in \gls{nlp}. The use of such methods requires the collection and annotation of a significant amount of medical data. However, \citet{Fries2022DatasetModeling} proposes the term "\emph{dataset debt}," highlighting that learning data in the biomedical field is poorly accessible, poorly documented, and opaque as to its reusability in a commercial or a hospital context. According to the article, only 13\% of the 167 analyzed datasets are accessible and downloadable, 22\% use a standard structured format, and 40\% are in the public domain.
In recent years, large language models (\glspl{llm}) have proved their ability to perform a wide range of tasks with high accuracy in a zero-shot or a few-shots contexts.
This trend holds great potential for clinical \gls{nlp}, as preliminary results show promise for information extraction tasks \citep{Agrawal2022LargeExtractors}. 
However, the clinical domain presents unique challenges due to the confidential and linguistically specific nature of its data, which can make collection and annotation time-consuming and expensive.
Using \glspl{llm} for efficient information extraction without training data could be attractive, but it raises confidentiality concerns.
The model deployment should be controllable, and the model's predictions should evolve to fit a specific and changing annotation guideline.
Most multilingual \glspl{llm} are not freely available \citep{Scao2022BLOOM:Model,ouyang2022training,Thoppilan2022LaMDA:Applications}, to the best of our knowledge, only BLOOM is open-source and deployable in a custom infrastructure. The computing resources to use these models remains challenging for healthcare establishments.     

One approach to address these issues is to distil \glspl{llm} into a smaller model via weak supervision. 
Weak supervision has recently gained community attention because it alleviates the annotation task. This technique refers to annotating datasets using rule-based, heuristic, dictionary extraction or more advanced methods and then training the smaller model on this dataset.
In the same way, knowledge distillation aims to transfer knowledge from a master model to a student model. It has often been used to compress large-scale models to improve memory footprint and the inference speed \citep{Li2021DynamicModels}. 
Moreover, student models trained through knowledge distillation can be more easily monitored and versioned. Hosting them increases the healthcare centre's sovereignty, and they become more compliant with existing privacy policies, as input data or predictions don't leave the building.

\section{Motivation and Contributions}
We study the use of \glspl{llm} in the knowledge distillation technique via weak supervision in the Multilingual Clinical domain, especially in clinical entity extraction. We extend the \citet{Agrawal2022LargeExtractors} study in the sense that we propose an in-depth study of the use of \instruct to annotate a training dataset. Our work\footnote{codebase: \url{https://github.com/arkhn/bio-nlp2023}.} mainly aims to compare the annotation quality using weak supervision tasks on a smaller model (Figure \ref{fig: motiv}). Finally, we propose to combine annotations provided by \instruct and the dictionary extraction method.

This takes form in these contributions:
\begin{itemize}
    \item We show that \llmmodel distillation (Figure \ref{fig: motiv} middle) is a competitive technique compared to classic weak-supervision techniques in a multilingual clinical domain;
    \item We propose a \hybridmodel approach (Figure \ref{fig: motiv} bottom) that combines annotations from \dict and \instruct, which outperforms the approach with only \instruct annotation;
    \item We provide an in-depth analysis on French clinical entity extraction, demonstrating how annotation method discrepancies affect model performance.
\end{itemize}

\begin{figure}[htp!]
\centering
\includegraphics[width=1\textwidth]{figures/multi_weak_supervision/workflow.pdf}
\caption{The different workflows we experiment with. The last workflow used a combination of \instruct and dictionary annotations; we tested different proportions of these annotations.}
\label{fig: motiv}
\end{figure}

\section{Related Works}
\paragraph{Weak Supervision} deep learning approach has achieved remarkable success in several domains beyond \gls{nlp} \citep{Zhang2022ASupervision}. However, the main bottleneck is collecting massively annotated data. To address this issue, weak supervision replaces ground-truth annotation with automatic annotation based on heuristic rules, gazetteers or constraints linguistic rules to address. Some techniques called \textit{distant supervision} exploit semantic links from knowledge bases or ontologies \citep{Lison2021Skweak:NLP}. \citet{Karamanolakis2021Self-TrainingSupervision} proposes an iterative self-training method to combine classic weak supervision and inference of the learning model to extract entities not covered by the initial heuristic rules. In the clinical domain, weak supervision has already been used for specific use cases \citep{Cusick2021UsingIdeation,Fries2021Ontology-drivenRecords,Wang2019ARepresentation}.
\paragraph{Clinical Language Models} In the clinical context, some specific terms are underrepresented or absent in the general domain. As a result, the clinical \gls{nlp} community has pretrained Language Models (LMs) \citep{Alsentzer2019PubliclyEmbeddings, Lee2020BioBERT:Mining,Alsentzer2019PubliclyEmbeddings} over domain-specific corpora (i.e. \gls{mimic}-III \citep{Johnson2016MIMIC-IIIDatabase}, Pubmed abstracts). These models could be trained from scratch or from checkpoint to specialize a domain-agnostic model \citep{Gururangan2020DontTasks}.

Though, the performance gains are marginal compared to the general language model. The structure and the abbreviated text present in clinical notes hurt performance.
Instead of pretraining a specialized clinical model, machine learning practitioners can fine-tune agnostic-domain \glspl{llm} such as the \gls{gpt} family of models or T5 on the clinical task. Fine-tuned general-purpose models have proven effective in clinical question-answering, protected health information de-identification, and relation extraction \citep{Lehman2023DoModels}. But this approach requires an important infrastructure and a regular re-finetuning if the data distribution of the \gls{ehr} shifts. Nevertheless, some \glspl{llm} have been trained from scratch over clinical domain-specific notes such as GatorTron \citep{yang2022large}, BioGPT \citep{Luo2022BioGPT:Mining} or ClinicalT5 \citep{Lu2022ClinicalT5:Text} who achieved promising performance on several tasks.
Additionally, in-context learning with agnostic \glspl{llm} such as \instruct \citep{ouyang2022training} where no weight is modified shows good results \citep{Agrawal2022LargeExtractors, Brown2020LanguageLearners} and outperforms specialized smaller models on several clinical tasks.
\paragraph{Prompt-based Method}
Prompt-based learning for generative language model treats a downstream task as a language modelling problem where a language model predicts the next tokens of the instruction given a textual prompt \citep{Sainz2021LabelExtraction}.

In this paradigm, instead of fine-tuning a model to a downstream task (\textit{"pre-train, fine-tune and predict"}), we manipulate the behaviour of a pretrained \gls{lm} using an appropriate prompt to give the desired output (\textit{"pre-train, prompt and predict"}). 
\textit{prompt engineering} explores the most suitable prompt method applied to a \gls{lm} to solve a task.
This way, an unsupervised pre-trained \gls{lm} can be used for many tasks \citep{Liu2023Pre-trainProcessing}.

Among these methods, \textit{in-context learning} is the most popular method for information extraction, question-answering or sentiment analysis. In the clinical domain, some works exist on information retrieval and question-answering. The prompt contains three components: the examples' template, the set of examples and the ordering of prompts, such as present in Figure \ref{fig:prompt}. The aim is to provide some training examples in the prompt before the test example. However, the chosen examples and their ordering and the format could impact performance \citep{Zhao2021CalibrateModels}; these three components must be tuned to optimize performance.

In another way, we can cite works around the chain of thoughts (\gls{cot}). This encourages the \gls{llm} to explain its reasoning to get more accurate results, especially in mathematical and logical reasoning \citep{Wei2022ChainModels, Cobbe2021TrainingProblems}. This technique could be used with reason edited manually in the prompt or with two separate prompts where the first involves a reasoning task, then concatenated with the second prompt involving the main tasks.

Other techniques involve generated knowledge similar to \gls{cot}. Instead of reasoning, the first prompt generates potentially useful information associated with the tasks concatenated in the final prompt \citep{Liu2022GeneratedReasoning}.

\section{Method}

\subsection{Creating Annotations and Knowledge Distillation via Weak Supervision}
\paragraph{Annotation Extraction from \gls{llm} Output}
Our study is inspired by the method developed in this paper \citep{Agrawal2022LargeExtractors}. Their works benchmark how \instruct \citep{ouyang2022training} perform clinical \gls{nlp} tasks in English. They show that \instruct performs well in several clinical tasks. They introduce 3 new datasets to benchmark few-shot clinical information extraction to achieve this. Also, they introduce \textit{guided prompt design} to induce easy-to-structure output with resolvers (or parsers) to convert the output into a structured prediction easily. 
Our work differs in the following: 
\begin{enumerate}
    \item Our studies areas are knowledge distillation via weak supervision and the improvement of this technique combining annotations from \glspl{llm} and dictionary extraction;
    \item our methods are applied in a multilingual context, the initial work was only done in English;
    \item we focus on the clinical entities extraction task based on the E3C dataset guidelines.
\end{enumerate}


In this work, the \gls{llm} is used only as a predictor; we only query the model, no additional fine-tuning step has been realized, and we can only access inference parameters such as temperature, top p, frequency or presence penalty. We set the \textit{temperature} and \textit{top p} to 0 to control randomness and have a deterministic behaviour. So as not to penalize repetitions, we set the \textit{presence penalty} and \textit{frequency penalty} to 0. We use an InstructGPT-3 model (text-davinci-003) \citep{ouyang2022training} to infer the whole annotations for all our experiments.
We provide the model with an instruction concatenated by the example to be predicted (Figure \ref{fig:prompt}). The output of \instruct is a string of characters that we must structure to align the predicted clinical entities with the initial text (Figure~\ref{fig:formalization}).

 The task is to annotate the words (or tokens) of a sentence $x \in \Sigma^*$ with a set of labels such that $L=\{O, B_\text{clin}, I_{\text{clin}}\}$ where O denotes a word in the text without a label, \bclin the first word of a clinical entity and \iclin the following words according to the format \textit{\gls{iob}} \citep{Ramshaw1995TextLearning}. The goal is to identify the labels O, \bclin, \iclin and their character offsets in the sentence $x$. The task output is defined as $\hat{y}=[ y_1, y_2, \ldots, y_n ] \in Y$, where $\hat{y}$ is the set of predicted annotations, and $y_i= \langle s_i, e_i, l_i
\rangle$ such that $s_i$ is the start offset, $e_i$ is the end offset and $l_i\in L$ of the $i^{th}$ annotation.
\input{figures/multi_weak_supervision/formalization}

As mentioned, a prompt-based method requires concatenating a template $t \in \Sigma^*$ with our sentence $x$ to give our prompt, such as $p=concat(t,x)$. 
We produce our output $o \in \Sigma^*$ from our \gls{llm} model $\Phi$ such as $o=\Phi(p, \theta_h)$, where $\theta_h$ represents the set of hyperparameters (\textit{temperature}, \textit{top p}, \textit{frequence penalty}, \textit{presence penalty}).

Then, we structure $o$ such as $\Sigma^* \rightarrow Y$ using a simple string-matching function to produce a set of labels $\hat{y}$ where $r$ is our resolver applying the string matching function: $r(o, x)=\hat{y}$.

\paragraph{Knowledge Distillation via Weak Supervision} Finally, the annotations generated via \instruct prediction are used as a training dataset to fine-tune a smaller language model to a \gls{ner} downstream task. For smaller language models, we limit our study to encoder models as mentioned in Table~\ref{tab:models}.

\subsection{Prompting}

We prime the model with three annotated data points, each corresponding to a sentence from our corpus (Table \ref{tab:counts}).
For each language, we try 3 sets of data points. For each of them, we test the F1-Score performance of \instruct on the test dataset (\gold), and we select the set with the best F1-Score to perform prediction on the unannotated dataset.
We insert keywords associated with the E3C guideline definition of the clinical entities into prompts.
We add guidance to explicit the response structure to facilitate parsing the output \citep{Agrawal2022LargeExtractors} (Figure \ref{fig:prompt}).

\input{figures/multi_weak_supervision/prompt}

\section{Experiments}
\subsection{Dataset}
We use the annotated E3C multilingual dataset \citep{Magnini2020TheCases} for our experiments, consisting of two annotation types: temporal and clinical entities. The languages supported are English (en), Basque (eu), Spanish (es), French (fr) and Italian (it). Clinical entities are identified as patient disorders which could map to the \gls{umls} meta thesaurus. The annotators have linked extracted clinical entities and \gls{umls} concepts. In our experiments, we only extract clinical entities without mapping \gls{umls} concepts. 

The E3C dataset is organized into 3 layers, each representing a different annotation quality level:
\begin{itemize}
    \item \gold: fully manual annotation, used as test set for all our experiments;
    \item \silver: semi-automatic annotation via \dict, used as training set. The dictionary contains terms from \gls{umls} and terms extracted from \gold;
    \item \silverval: a subset of \silver (approximately $10\%$) that has been manually corrected;
    \item a third unannotated layer that we don't use for our experiments.
\end{itemize}

The E3C dataset is well-suited for our weak-supervision studies, but has limited data in its various layers (Table \ref{tab:counts}). To address this limitation, we divide \silver into five parts using 5-fold cross-validation. For \silverval, we use the entire data as the training set. Our experiments employ multiple models for each language and rely solely on \verb|xlm-roberta-base| in a \multi context. The results presented in our work are an aggregation of the means and standard deviations across models and folds. Each experiment result and model are reported in Appendix \ref{appendix:weak_supervision}.

\subsection{Experimental Setup} 
\label{sec:prot}

We conduct experiments on clinical entity extraction using encoder models as student models for knowledge distillation (Table~\ref{tab:models}).

\begin{table}
    \centering
    \small
    \resizebox{0.8\textwidth}{!}{
        \input{figures/multi_weak_supervision/models}
    }
    \caption{Student models used for each language, referenced by their HuggingFace identifier.}
    \label{tab:models}
\end{table}

We compare three supervision strategies: \dictmodel (trained on \dict annotations), \llmmodel (trained on \instruct annotations), and \hybridmodel (trained on a mix of both). We control the annotation mix using a ratio \rmix, where $\rmix=0$ corresponds to \dictmodel and $\rmix=1$ to \llmmodel.

We evaluate these strategies across different data configurations:

\paragraph{Training on \silver.} We compare \dictmodel and \llmmodel models trained on the full \silver dataset to establish baseline performance for each annotation method in a \mono context.

\paragraph{Training on \silverval.} We use the smaller, manually corrected \silverval as training data, comparing \dictmodel and \llmmodel to assess performance with limited but higher-quality supervision.

\paragraph{Silver Mixed Annotations.} We train on \silver but replace weak-supervision annotations with manual corrections from \silverval where available, combining the volume of weak supervision with targeted manual refinement.

\paragraph{Hybrid Supervised.} We vary \rmix $\in [0,1]$ to mix \dict and \instruct annotations on \silver, investigating whether combining heterogeneous annotation sources improves generalization.

\paragraph{Multilingual transfer.} We apply the same mixing strategy but concatenate training data from all five languages, using \verb|xlm-roberta-base| as the student model in a \multi context.

\section{Results}

\subsection{Annotation Analysis} 

We observe that \instruct extracts more entities than \dict on \silver (Table~\ref{tab:counts}). This trend is reduced in English and Spanish, but we observed a higher quantity of \iclin tokens annotated by \instruct for all languages. On \silverval, the quantity of tokens annotated by both methods is relatively equivalent, likely due to human validation.

\begin{table}
    \centering
    \small
    \resizebox{0.85\textwidth}{!}{
        \input{figures/multi_weak_supervision/counts}
    }
    \caption{Number of annotated tokens for each annotation type on \silver and \silverval. \textit{Gold} corresponds to \dict annotation, \textit{\gls{gpt}} to \instruct annotation.}
    \label{tab:counts}
\end{table}

\subsection{Knowledge Distillation Evaluation} 

We compare the \llmmodel models and direct \instruct inference on the \gold test set (Table \ref{tab:distilation}). Distillation is beneficial for Basque, French and Italian, with a remarkable gap in Italian ($0.63$ for \instruct vs $0.75$ for \llmmodel). Spanish and English show reversed trends: \instruct performs better than distilled models, echoing the annotation patterns observed above.

\begin{table}
    \centering
    \small
    \input{figures/multi_weak_supervision/distilation_instruct}
    \caption{Mean F1-score on \gold comparing direct \instruct output and \llmmodel models for each language.}
    \label{tab:distilation}
\end{table}

\subsection{Monolingual Results}

\paragraph{Baseline comparison on \silver.} Comparing global F1-Score (Table \ref{tab:global}), \llmmodel models outperform \dictmodel models overall. The \llmmodel approach displays better recall and recognizes multi-word clinical terms more easily, though this flexibility increases false positives and lowers precision. Figure \ref{fig:setting_A} shows a noticeable performance gain of almost $0.1$ for Basque, followed by French and Italian. In English, both models perform similarly. For Spanish, \dictmodel outperforms \llmmodel and achieves our highest F1-Score.

\begin{figure}
    \centering
    \includegraphics[width=0.6\textwidth]{figures/multi_weak_supervision/plot_A.pdf}
    \caption{Mean F1-score comparison between \llmmodel (orange) and \dictmodel (blue) models trained on \silver, by language.}
    \label{fig:setting_A}
\end{figure}

\paragraph{Training on \silverval.} The limited amount of annotated tokens in \silverval (Table \ref{tab:counts}) hurts \llmmodel performance ($0.61$ vs $0.70$ on \silver), while \dictmodel performance slightly improves ($0.70$ vs $0.67$). With limited data, the \dictmodel approach proves more stable, while \llmmodel recall drops significantly ($0.58$ vs $0.68$ on \silver).

\paragraph{Silver Mixed Annotations.} Mixing a small proportion of manual annotations from \silverval with \silver improves both models (Table \ref{tab:global}). The \llmmodel approach outperforms \dictmodel with a gain of $0.03$. In both cases, the F1-Score gain is due to improved recall ($+0.05$ compared to training on \silver alone).

\paragraph{Hybrid Supervised.} For all languages except Basque, we obtain better results when mixing \dict and \instruct annotations. The optimal ratio is reached around $\rmix \in [0.4, 0.6]$ (Figure \ref{fig:confuse}). Basque does not follow this trend: using only \instruct annotations ($\rmix = 1$) gives the best results, likely because the original \dict annotations were created using a low-resource lexicon (only 63 \iclin tokens vs 482 for \instruct).

\begin{figure}[t!]
    \begin{subfigure}[t]{0.5\textwidth}
        \centering
        \includegraphics[height=2.3in]{figures/multi_weak_supervision/conf_D.pdf}
        \caption{\mono context}
    \end{subfigure}
    \begin{subfigure}[t]{0.5\textwidth}
        \centering
        \includegraphics[height=2.3in]{figures/multi_weak_supervision/conf_E.pdf}
        \caption{\multi context}
    \end{subfigure}
    \caption{Mean F1-score as a function of the hybrid supervision ratio \rmix. $\rmix=0$: \dictmodel only; $\rmix=1$: \llmmodel only. Each line represents a language.}
    \label{fig:confuse}
\end{figure}

\begin{table}
    \centering
    \resizebox{1\textwidth}{!}{
        \input{figures/multi_weak_supervision/global}
    }
    \caption{F1-score, Precision and Recall for different training configurations. $\rmix=1$ corresponds to \llmmodel, $\rmix=0$ to \dictmodel.}
    \label{tab:global}
\end{table}

\subsection{Multilingual Results}

Using a \multi training set with \verb|xlm-roberta-base| gives inferior results compared to \mono training (Table \ref{tab:global_d}). Italian is the exception, with a slight improvement ($+0.01$) at $\rmix = 0.8$. For other languages, hybrid supervision does not improve results as observed in \mono settings, likely due to noise introduced by the multilingual nature of the training data.

\begin{table}
    \centering
    \small
    \resizebox{0.7\textwidth}{!}{
        \input{figures/multi_weak_supervision/global_d}
    }
    \caption{F1-score, Precision, and Recall for \mono and \multi contexts. Scores are aggregated across languages, with $r_{max}$ representing the optimal \rmix value.}
    \label{tab:global_d}
\end{table}

Detailed results for each language and model, including all evaluated configurations (Settings A through E), are provided in Appendix~\ref{appendix:weak_supervision}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% IN-DEPTH FRENCH CASE STUDY
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{In-depth Analysis: French Clinical Entity Extraction}
\label{sec:french_case_study}

To better understand the differences between annotation methods and their impact on model performance, we conduct a detailed analysis on the French subset of E3C. French represents our primary deployment context for clinical \gls{nlp} applications. We use \textit{camembert-base} \citep{Martin2019CamemBERT:Model} as the student model for all French experiments.

\subsubsection{Annotation Discrepancy Analysis}

\instruct extracts almost twice as many entities as \dict (Table \ref{tab:healthnlp_counts}). This gap is more pronounced on \silver, while on \silverval the difference is reduced due to human validation. This discrepancy arises because \instruct has no access to the E3C guidelines: the prompt instructs it to extract "\textit{disorders}", "\textit{disease}", or "\textit{symptoms}", which is less restrictive than the E3C annotation schema.

\begin{table}
    \centering
    \small
    \resizebox{0.85\textwidth}{!}{
        \input{figures/weak_supervision_healthnlp_2023/counts}
    }
    \caption{Number of annotated tokens for each annotation type across French E3C layers.}
    \label{tab:healthnlp_counts}
\end{table}

The confusion matrices (Figure \ref{fig:healthnlp_confuse}) provide deeper insight. Both methods label approximately the same proportion of tokens as O ($0.93$), indicating that \instruct annotations are coherent with the task. Only $0.04$ of \bclin tokens are mislabeled as \iclin, demonstrating good discrimination between begin and inner tokens.

The key finding is that \instruct tends to classify O tokens as inner tokens, resulting in longer extracted entities.
This behavior stems from the semantic gap between E3C's strict annotation schema and the broader medical concepts conveyed in the prompt.
While E3C defines precise entity boundaries based on clinical relevance, \instruct captures entities based on linguistic coherence, often extending spans to include adjectives, severity markers, or anatomical localizations.
This mismatch explains the higher token count and suggests that prompt engineering alone cannot fully replicate corpus-specific annotation conventions.

\begin{figure}[t!]
    \begin{subfigure}[t]{0.5\textwidth}
        \centering
        \includegraphics[height=2in]{figures/weak_supervision_healthnlp_2023/fr.layer2.validation_heatmap}
        \caption{\silverval}
        \vspace{4mm}
    \end{subfigure}
    \begin{subfigure}[t]{0.5\textwidth}
        \centering
        \includegraphics[height=2in]{figures/weak_supervision_healthnlp_2023/fr.layer2_heatmap}
        \caption{\silver}
    \end{subfigure}
    \caption{Confusion matrices between \dict and \instruct annotations for French. Blue cells highlight where \instruct annotates more entities than \dict.}
    \label{fig:healthnlp_confuse}
\end{figure}

\subsubsection{Detailed Performance by Entity Type}

Comparing the two approaches reveals complementary strengths. The \dictmodel achieves better F1-Score on \bclin (Table \ref{tab:healthnlp_layer2}), excelling at one-word clinical terms but struggling with multi-word entities (lower recall for \iclin).

The \llmmodel demonstrates better recall by recognizing multi-word terms more easily, at the cost of precision due to over-detection.

\input{figures/weak_supervision_healthnlp_2023/04_experiment_table_layer2}

With limited training data (\silverval), the \dictmodel suffers more ($0.57$ vs $0.69$ on \silver), while \llmmodel maintains stable performance with increased variance across folds (Table \ref{tab:healthnlp_layer2validate}).

\input{figures/weak_supervision_healthnlp_2023/04_experiment_table_layer2_validate}

The \silvermixedmodel approach improves both methods (Table \ref{tab:healthnlp_layer2blended}). The \dictmodel shows notably better recall for \iclin ($0.35$ vs $0.24$), while \llmmodel gains precision on both \bclin and \iclin. Manual annotations help calibrate each method's tendency toward under- or over-extraction.

\input{figures/weak_supervision_healthnlp_2023/04_experiment_table_layer2_blended}

\subsubsection{Optimal Mixing Ratio Analysis}

The French experiments allow detailed examination of the mixing strategy (Figure \ref{fig:healthnlp_scatter}). A ratio of $\rmix=0.5$ improves F1-Score by $0.03$ compared to $\rmix=1$. Ratios above $0.5$ also provide better stability across cross-validation folds.

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{figures/weak_supervision_healthnlp_2023/scatters.pdf}
    \caption{F1-score as a function of \rmix for French. Blue dots: individual folds; orange dots: mean. Shaded area: standard deviation.}
    \label{fig:healthnlp_scatter}
\end{figure}

Table \ref{tab:healthnlp_global} summarizes French results. The \hybridmodel approach with $\rmix=0.5$ achieves the best F1-score ($0.76$), with a reduced gap between Recall ($0.81$) and Precision ($0.73$), confirming that combining annotation methods provides beneficial diversity.

\begin{table}
    \centering
    \small
    \resizebox{0.7\textwidth}{!}{
        \input{figures/weak_supervision_healthnlp_2023/global}
    }
    \caption{F1-score, Precision and Recall for French experiments.}
    \label{tab:healthnlp_global}
\end{table}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% DISCUSSION
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Discussion}

Our experiments demonstrate the potential of knowledge distillation and weak supervision for clinical entity extraction. The \llmmodel approach outperforms \dictmodel in most languages, especially for Basque, French, and Italian.

\paragraph{Language-specific variations.} Spanish shows reversed trends, with \dictmodel performing better. This may be due to differences in data sources: while other languages use Pan African Journal or Pubmed corpora, Spanish uses SPACCC, with different clinical entity distributions. Additional preprocessing (sentence removal, capitalization changes) may reinforce these differences.

In English, both approaches perform similarly, likely due to superior \dict annotation quality from more extensive \gls{umls} resources.

\paragraph{Benefits of hybrid supervision.} The \hybridmodel approach outperforms single-source supervision for most languages. The French case study (Section \ref{sec:french_case_study}) provides detailed evidence: \dict and \instruct have complementary error patterns. \dict tends to under-extract (missing multi-word entities), while \instruct over-extracts (longer spans). Combining them at $\rmix=0.5$ balances these biases.

Basque is an exception where $\rmix=1$ works best, because the original \dict annotations used a low-resource lexicon (only 63 \iclin tokens vs 482 for \instruct).

\paragraph{Multilingual limitations.} The \multi setting did not yield significant improvements. Romance language predominance may introduce noise for English and Basque. Basque, as a language isolate with unique structures, is particularly affected by linguistically distant training data.

\paragraph{Annotation quantity vs. quality.} \instruct extracts almost twice as many entities as \dict, primarily by extending entity boundaries rather than detecting new entities. The French analysis (Figure \ref{fig:healthnlp_confuse}) confirms this pattern. Human validation in \silverval reduces this gap, suggesting that \instruct's broader extraction could be refined with targeted manual correction.

\section{Limitations}
One limitation is the small test set size, which may impact generalizability. We also focused solely on clinical entity extraction; future work could investigate temporal entity extraction and relation extraction using other E3C layers.

The E3C guidelines were designed for entity-linking via \gls{umls}, leading to annotation choices that may not reflect natural entity boundaries. Clinical entities may be split into separate disorder concepts or reduced to fit \gls{umls} entries \citep{Magnini2020TheCases}, introducing biases that affect span prediction.

\section{Conclusion}
Our results demonstrate that \llmmodel distillation outperforms \dictmodel supervision for clinical entity extraction in most languages. The \hybridmodel approach, combining both annotation sources, further improves performance by leveraging their complementary strengths: \dict provides precise but narrow coverage, while \instruct offers broader but noisier annotations.

This weak-supervision approach with \glspl{llm} reduces annotation costs while enabling deployment of small to medium-sized models that can be hosted locally, preserving data confidentiality and reducing energy costs. For low-resource languages like Basque, \glspl{llm} offer a competitive alternative when linguistic resources for \dict are limited.

Future work will investigate confidence-based annotation combination, using prediction uncertainty to select annotations. Ensemble approaches with multiple \glspl{llm} could increase annotation diversity. We will also explore adapting Chain-of-Thought prompting for clinical entity extraction, potentially improving \instruct precision by incorporating E3C annotation guidelines into multi-step prompts.
